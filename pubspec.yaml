name: compass
description: A compass ap.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_compass: ^0.5.1
  permission_handler: ^5.1.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.3.0

flutter:
  uses-material-design: true

  assets:
    - assets/compass.png